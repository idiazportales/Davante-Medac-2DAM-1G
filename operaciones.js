export const sumar = (a, b) => {
    // Alumno 1 implementará esto
};

export const restar = (a, b) => {
    // Alumno 2 implementará esto
};

export function multiplicar(a, b) {
    return a * b;
}

export const dividir = (a, b) => {
    return a / b;
};